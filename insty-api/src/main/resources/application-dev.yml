spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

swagger:
  server-url: ${SWAGGER_SERVER_URL}

video:
  upload:
    max-minute:
      course: 60
      answer: 60
      question: 60

mixpanel:
  enabled: false
  token: ""
  verbose: false
  strict: false