spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

swagger:
  server-url: ${SWAGGER_SERVER_URL}

video:
  upload:
    max-minute:
      course: 60
      answer: 60
      question: 60

kakaopay:
  timeout:
    connectMillis: 5000
    readMillis: 10000